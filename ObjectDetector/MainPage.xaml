<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:skia="clr-namespace:SkiaSharp.Views.Forms;assembly=SkiaSharp.Views.Forms"
             xmlns:local="clr-namespace:ObjectDetector" 
             x:Class="ObjectDetector.MainPage"
             Padding="20"
             BackgroundColor="DarkSlateGray">
    <ContentPage.BindingContext>
        <local:MainViewModel/>
    </ContentPage.BindingContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <skia:SKCanvasView x:Name="ImageCanvas" PaintSurface="OnCanvasViewPaintSurface"/>
        <Button Grid.Row="1" Text="Take photo" BackgroundColor="Green" 
                TextColor="White" Margin="0,20,0,0" Command="{Binding TakePhotoCommand}"
                IsEnabled="{Binding IsEnabled}"/>
    </Grid>
</ContentPage>